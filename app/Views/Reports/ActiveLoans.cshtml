@model IEnumerable<KutuphaneOtomasyonu.Controllers.ActiveLoanRow>
@{
    ViewData["Title"] = "Aktif Ödünçler";
}

<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header bg-info text-white">
                    <h3 class="card-title mb-0">
                        <i class="fas fa-book-reader me-2"></i>
                        Aktif Ödünçler
                    </h3>
                </div>
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table table-striped table-hover">
                            <thead class="table-dark">
                                <tr>
                                    <th>Üye</th>
                                    <th>Kitap</th>
                                    <th>Vade Tarihi</th>
                                    <th>Durum</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var r in Model)
                                {
                                    <tr>
                                        <td>@r.member_name</td>
                                        <td>@r.book_title</td>
                                        <td>@r.due_at.ToString("dd.MM.yyyy HH:mm")</td>
                                        <td>
                                            @if (r.delay_days > 0)
                                            {
                                                <span class="badge bg-danger">@r.delay_days gün gecikmiş</span>
                                            }
                                            else
                                            {
                                                <span class="badge bg-success">Gününde</span>
                                            }
                                        </td>
                                    </tr>
                                }
                                @if (!Model.Any())
                                {
                                    <tr>
                                        <td colspan="4" class="text-center text-muted">Aktif ödünç bulunmamaktadır.</td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                    <div class="mt-3">
                        <a class="btn btn-secondary" asp-controller="Reports" asp-action="Index">
                            <i class="fas fa-arrow-left me-1"></i>
                            Geri Dön
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>




