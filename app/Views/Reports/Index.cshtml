@using KutuphaneOtomasyonu.Services
@inject AuthService AuthService
@{
    ViewData["Title"] = "Raporlar";
    var isAdmin = AuthService.IsAdmin();
    var isLoggedIn = AuthService.IsLoggedIn();
}

<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header bg-primary text-white">
                    <h3 class="card-title mb-0">
                        <i class="fas fa-chart-bar me-2"></i>
                        Kütüphane Raporları
                    </h3>
                </div>
                <div class="card-body">
                    <div class="row g-4">
                        @if (isAdmin)
                        {
                            <div class="col-md-6 col-lg-3">
                                <div class="card bg-info text-white h-100">
                                    <div class="card-body d-flex flex-column">
                                        <div class="d-flex justify-content-between mb-3">
                                            <div>
                                                <h4 class="card-title">Aktif Ödünçler</h4>
                                                <p class="card-text mb-0">Şu anda ödünç verilen kitaplar</p>
                                            </div>
                                            <div class="align-self-center">
                                                <i class="fas fa-book-reader fa-2x"></i>
                                            </div>
                                        </div>
                                        <a asp-action="ActiveLoans" class="btn btn-light btn-sm mt-auto">
                                            <i class="fas fa-eye me-1"></i>
                                            Raporu Görüntüle
                                        </a>
                                    </div>
                                </div>
                            </div>
                        }

                        <div class="col-md-6 col-lg-3">
                            <div class="card bg-success text-white h-100">
                                <div class="card-body d-flex flex-column">
                                    <div class="d-flex justify-content-between mb-3">
                                        <div>
                                            <h4 class="card-title">En Çok Ödünç Alınan Kitaplar</h4>
                                            <p class="card-text mb-0">Popüler kitaplar listesi</p>
                                        </div>
                                        <div class="align-self-center">
                                            <i class="fas fa-trophy fa-2x"></i>
                                        </div>
                                    </div>
                                    <a asp-action="TopBooks" class="btn btn-light btn-sm mt-auto">
                                        <i class="fas fa-eye me-1"></i>
                                        Raporu Görüntüle
                                    </a>
                                </div>
                            </div>
                        </div>

                        @if (isAdmin)
                        {
                            <div class="col-md-6 col-lg-3">
                                <div class="card bg-warning text-dark h-100">
                                    <div class="card-body d-flex flex-column">
                                        <div class="d-flex justify-content-between mb-3">
                                            <div>
                                                <h4 class="card-title">Üye Ödünç Sayıları</h4>
                                                <p class="card-text mb-0">Üyelerin ödünç istatistikleri</p>
                                            </div>
                                            <div class="align-self-center">
                                                <i class="fas fa-users fa-2x"></i>
                                            </div>
                                        </div>
                                        <a asp-action="MemberLoanCounts" class="btn btn-dark btn-sm mt-auto">
                                            <i class="fas fa-eye me-1"></i>
                                            Raporu Görüntüle
                                        </a>
                                    </div>
                                </div>
                            </div>
                        }

                        @if (isAdmin)
                        {
                            <div class="col-md-6 col-lg-3">
                                <div class="card bg-secondary text-white h-100">
                                    <div class="card-body d-flex flex-column">
                                        <div class="d-flex justify-content-between mb-3">
                                            <div>
                                                <h4 class="card-title">Rezervasyonlar</h4>
                                                <p class="card-text mb-0">Bekleyen rezervasyonlar</p>
                                            </div>
                                            <div class="align-self-center">
                                                <i class="fas fa-clock fa-2x"></i>
                                            </div>
                                        </div>
                                        <a asp-action="Reservations" class="btn btn-light btn-sm mt-auto">
                                            <i class="fas fa-eye me-1"></i>
                                            Raporu Görüntüle
                                        </a>
                                    </div>
                                </div>
                            </div>
                        }
                    </div>

                    <div class="row g-4 mt-2">
                        @if (isAdmin)
                        {
                            <div class="col-md-6 col-lg-3">
                                <div class="card bg-dark text-white h-100">
                                    <div class="card-body d-flex flex-column">
                                        <div class="d-flex justify-content-between mb-3">
                                            <div>
                                                <h4 class="card-title">Sistem Logları</h4>
                                                <p class="card-text mb-0">Audit log kayıtları</p>
                                            </div>
                                            <div class="align-self-center">
                                                <i class="fas fa-file-alt fa-2x"></i>
                                            </div>
                                        </div>
                                        <a asp-action="AuditLog" class="btn btn-light btn-sm mt-auto">
                                            <i class="fas fa-eye me-1"></i>
                                            Raporu Görüntüle
                                        </a>
                                    </div>
                                </div>
                            </div>
                        }

                    </div>

                    <div class="row mt-4">
                        <div class="col-12">
                            <div class="alert alert-info">
                                <i class="fas fa-info-circle me-2"></i>
                                <strong>Bilgi:</strong> Raporlar gerçek zamanlı olarak güncellenir. Detaylı analiz için ilgili raporu seçin.
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
